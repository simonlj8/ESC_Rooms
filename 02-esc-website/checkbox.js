/* function () {
    function checkbox() {
        let rating_from = document.getElementById("challenges-list");
        for (let i = 0; i < challenge.length; i++) {
          let item = `<li class="challenges-item">
} */

/* function check_check()
{
    if (document.getElementById('rat05').checked) 
    {
        const cbs = document.querySelectorAll('input[name="rating_from"]');
        cbs.forEach((cb) => {
            cb.checked = checked;
        });
    }
} */

const checkbox = document.getElementById('rat05')

/* checkbox.addEventListener('change', (event) => {
  if (event.currentTarget.checked) { */

checkbox.addEventListener(currentTarget.checked) {

alert("checked");

const rat01 = document.getElementById("search");
console.log(rat01.checked);
};